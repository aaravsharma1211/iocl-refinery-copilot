# Local authentication configuration.
# This file is intentionally safe for the public repository.
# For local development, replace the demo password and cookie key locally.

credentials:
  usernames:
    demo_user:
      email: demo@example.com
      name: Demo User
      password: CHANGE_ME

cookie:
  expiry_days: 30
  key: CHANGE_ME_TO_A_RANDOM_SECRET
  name: enterprise_ai_cookie

preauthorized:
  emails: []
